# Cargo-installed tools
# prj-cli — disabled: builds from source and needs libssl-dev + pkg-config.
#           Re-enable after `sudo apt-get install -y libssl-dev pkg-config`.
# prj-cli
{{- if .isUbuntu }}
# Ubuntu-only: not available via apt
starship
du-dust
dufs
procs
sd
tokei
hyperfine
tealdeer
just
watchexec-cli
zoxide
{{- end }}
