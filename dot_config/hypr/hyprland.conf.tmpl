{{- if .isArch -}}
# Hyprland configuration — Arch
# TODO: populate with actual config

monitor=,preferred,auto,auto

input {
  kb_layout = us
}

general {
  gaps_in = 5
  gaps_out = 10
  border_size = 2
}
{{- end }}
